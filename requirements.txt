# Serving stack — no TensorFlow needed (see scripts/convert_model.py for the
# one-time legacy-model conversion, which has its own requirements).
fastapi>=0.111
uvicorn[standard]>=0.30
python-multipart>=0.0.9
pillow>=10.3
numpy>=1.26
torch>=2.3
onnx2torch>=1.5
